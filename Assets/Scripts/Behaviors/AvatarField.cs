using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// An empty struct, just used to tell the UI that we want to populate a
/// bundle reference with an Avatar, either from an asset, or from an instance
/// </summary>
public struct AvatarField { }
